{
  "name": "tiny-realms",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A 2D real-time strategy skirmish game built on a deterministic, command-driven simulation. Single-player vs AI now, multiplayer-ready architecture for the future.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
